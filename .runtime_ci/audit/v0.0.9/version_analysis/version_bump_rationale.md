- **Decision**: patch
The commit is a `fix` that updates the organization URL for the `encrypt` dependency and bumps the required versions for both `encrypt` and `runtime_isomorphic_library`. According to the versioning guidelines, dependency updates and bug fixes correspond to a patch release, as there are no changes to the public API of this package.

- **Key Changes**:
  - Updated the git repository URL for the `encrypt` package from `pieces-app/encrypt` to `open-runtime/encrypt`.
  - Bumped the `encrypt` dependency version from `^5.1.6` to `^6.0.0`.
  - Bumped the `runtime_isomorphic_library` dependency version from `^1.2.1` to `^2.0.0`.

- **Breaking Changes**: None

- **New Features**: None

- **References**:
  - Commit: `fix: correct org URLs and stale dependency versions`
