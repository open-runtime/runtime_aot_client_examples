* Transitioned the AWS Voxtral integration test into a robust, CI-safe unit test.
* Expanded the automated documentation generation to cover examples for `auth`, `client`, and `interceptor` modules.
* Upgraded CI workflows to the latest `runtime_ci_tooling` (`^0.12.0`) and added an auto-format validation step.
* Fixed the `create-release` rebase failure during the CI process.
